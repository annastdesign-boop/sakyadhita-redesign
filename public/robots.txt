# ─────────────────────────────────────────────────────────────
# PREVIEW MODE — crawling is BLOCKED.
# This site lives at sakyadhita-redesign.netlify.app and is not the
# public site yet. Blocking crawlers stops search-engine bots from
# repeatedly downloading every page + PDF (which was burning Netlify
# bandwidth credits) and keeps the preview out of Google results.
#
# ⚠️ AT LAUNCH (when sakyadhita.org points here): replace this whole
# file with the "Allow all" version and point the sitemap at
# https://sakyadhita.org/sitemap-index.xml
# ─────────────────────────────────────────────────────────────
User-agent: *
Disallow: /
